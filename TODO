TO-DO/soon:
- Use `createDebug()` instead of `TELEFUNC_SHIELD_DEBUG` (let's do it after no potential conflicts with pending PRs)

TO-DO/next-major-release:
- remove the deprecated `telefunc()` alias (now renamed to `serve()`)
