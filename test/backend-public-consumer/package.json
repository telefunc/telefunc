{
  "private": true,
  "type": "module",
  "scripts": {
    "test:released-api": "node check-released-exports.mjs && tsc -p tsconfig.json --module Node16 --moduleResolution Node16 && tsc -p tsconfig.json --module NodeNext --moduleResolution NodeNext && tsc -p tsconfig.json --module ESNext --moduleResolution Bundler --customConditions worker && tsc -p tsconfig.browser.json && tsc -p tsconfig.platforms.json"
  },
  "devDependencies": {
    "@telefunc/redis": "workspace:*",
    "telefunc": "workspace:*",
    "typescript": "^6.0.2"
  }
}
