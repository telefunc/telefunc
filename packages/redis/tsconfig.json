{
  "compilerOptions": {
    "outDir": "./dist/",
    "target": "ES2019",
    "module": "Node16",
    "moduleResolution": "Node16",
    "skipLibCheck": true,
    "declaration": true,
    "strict": true,
    "noUnusedParameters": true,
    "esModuleInterop": true,
    "noUncheckedIndexedAccess": true,
    "lib": ["DOM", "ESNext"]
  },
  "include": ["src/**/*.ts"],
  "// src/room": "The dark Room backend spike imports the telefunc SPI by source path (cross-package),",
  "// src/room (2)": "so it is excluded from this emit build (it is never shipped) and type-checked",
  "// src/room (3)": "separately via tsconfig.room.json (noEmit — rootDir is not enforced there).",
  "exclude": ["src/**/*.spec.ts", "src/room/fixture.ts", "src/room/register.ts"]
}
